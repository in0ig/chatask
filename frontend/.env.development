# 开发环境配置
VITE_APP_BASE_API=/api
VITE_API_TARGET=http://localhost:8000
# WebSocket 默认走当前页面 host + Vite /api 代理（远程访问勿写 localhost）
# 仅本机特殊调试需要直连后端时再取消下行：
# VITE_WS_DIRECT_URL=ws://127.0.0.1:8000
VITE_ENABLE_STREAMING=true
